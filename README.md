# To start the DASHBOARD:

- Make sure you are in folder "dashboard" (`cd dashboard`)
- Run it! (`python3 ./main.py`)


# To start the UPLOADER:
- Make sure you are in folder "dashboard" (`cd dashboard`)
- Run it! (`python3 ./uploader.py`)
